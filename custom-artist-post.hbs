{{!< default}}

{{#post}}
<main id="{{slug}}" class="mb-6">
    <article id="{{slug}}" class="mb-16">
        <div class="relative text-center overflow-hidden">
                <img class="filter brightness-50 object-cover w-full h-full hover:scale-105 transform transition motion-reduce:transform-none" 
                    srcset="{{img_url feature_image size="s"}} 300w,
                            {{img_url feature_image size="m"}} 720w,
                            {{img_url feature_image size="l"}} 960w"
                            sizes="800px"
                            src="{{img_url feature_image size="l"}}"
                            alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{name}}{{/if}}"
                            >
                <div class="absolute w-full h-full inset-y-0 lg:px-32 md:px-16 sm:px-4 px-2">
                    <div class="flex h-full justify-center items-center">
                        <h1 class="lg:text-7xl text-3xl uppercase">{{title}}</h1>
                    </div>
                </div>
                
        </div>
        <section class="gh-content gh-canvas container mt-14">
            {{content}}
        </section>
    </article>
    {{> rec}}
</main>
{{/post}}